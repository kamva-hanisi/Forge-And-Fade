const Booking = () => {
  return (
    <main className="page">
      <section>
        <div className="container">
          <span className="section-label">Reserve Your Chair</span>

          <h1 className="section-title">Book an Appointment</h1>

          <p className="section-description">
            Select your service, barber, date and preferred appointment time.
          </p>
        </div>
      </section>
    </main>
  );
};

export default Booking;